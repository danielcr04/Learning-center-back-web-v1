/*namespace Pc2_Tipo1.API.OTROS_CASOS.DOMAIN;

public interface IManagerRepository : IBaseRepository<Manager>
{
    /// <summary>
    /// Checks if a manager already exists with the same first and last name.
    /// </summary>
    /// <param name="firstName">First name of the manager</param>
    /// <param name="lastName">Last name of the manager</param>
    /// <returns>True if a manager exists with the same name, false otherwise</returns>
    Task<bool> ExistsByFullNameAsync(string firstName, string lastName);
}*/